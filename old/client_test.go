package conductor

import (
	"fmt"
	"testing"
)

func TestCreateClient(t *testing.T) {
	// server := conductor.CreateServer(8080)
	// go server.Start()
	// name := "hello"
	// c := CreateClient("ws://localhost:8080", "token1", "")
	// c.Bind(name, func(message Message) {
	// 	fmt.Println("got a message: ", message.Body)
	// 	return
	// })
	// sender := CreateClient("ws://localhost:8080", "token2", "")
	// sender.Bind(name, func(message Message) {
	// })
	// sender.SendMessage("hi there!", name, nil)
	t.Skip("need createClient test")
}
